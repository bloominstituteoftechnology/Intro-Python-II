document all steps of program to debug the problem:

loop through room

    if players room is current in loop print the name/desc
    if it has items:

        loop through items array and 
        print the item and room 
        cmd = ...
        




made check move function