# Write a class to hold player information, e.g. what room they are in
# currently.

class Player:
    """ A class to hold player information, e.g in what room they are currently in """
    def __init(self, name, current_room):
        self.name = name
        self. current_room = current_room